#include <stdio.h>

#define MAX 10000

int main(){
  int N, i;
  int a[MAX];

  for(i = 0; i < N; i++){
    scanf("%d", &a[i]);
  }

  for(i = 0; i < N; i++){
    if(a[i] % 2 != 0)
  }
  return 0;
}
