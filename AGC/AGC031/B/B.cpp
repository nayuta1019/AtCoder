#include <bits/stdc++.h>

using namespace std;

typedef long long llint;
const long long MOD = 1e9 + 7;

llint dp[202020];

int main() {
  int N;
  cin >> N;
  vector<int> c(N);
  for (int i = 0; i < N; i++) {
    cin >> c[i];
  }

  return 0;
}