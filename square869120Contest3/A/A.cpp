#include <bits/stdc++.h>

using namespace std;

int calendar[20][10];

int main() {
  int n, k;
  cin >> n >> k;
  if (n < 3) {
    cout << 0 << endl;
    return 0;
  }

  if (n <= 13) {
    for (int y = 1; y <= n; y++) {
      for (int x = 1; x <= 7; x++) {
      }
    }
  }

  return 0;
}